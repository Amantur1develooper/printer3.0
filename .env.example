SECRET_KEY=замените-на-длинный-случайный-ключ-минимум-50-символов
DEBUG=True

# sqlite — для локальной разработки, postgres — для продакшна
DB_ENGINE=sqlite
ALLOWED_HOSTS=localhost,127.0.0.1,ваш-домен.com

DB_NAME=onlineprinter
DB_USER=printer
DB_PASSWORD=printer123
DB_HOST=db
DB_PORT=5432

# Телефон для оплаты (показывается в QR-коде и на странице оплаты)
PAYMENT_PHONE=+996 700 123456
PAYMENT_RECIPIENT=Имя Фамилия

# Максимальный размер файла в МБ
MAX_FILE_SIZE_MB=50

# Минимальный порог бумаги (если меньше — заказ не принимается)
MIN_PAPER_THRESHOLD=5
